# Hive vibration architecture

`HiveVibrationEngine` reads a hive-local snapshot, applies configured deterministic physics and writes only hive-space placements, node states and reasoning records. A cloud remains the global identity; a placement is the mutable local instance. Resonance clusters are derived run data and are not global concepts.

