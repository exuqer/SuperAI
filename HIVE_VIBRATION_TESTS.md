# Vibration tests

Coverage includes zero-step runs, sequential multi-step state, local/global isolation, bounded values, immutable snapshots, deterministic configuration, JSON current/initial/trace exports and step diffs. Existing model and local-memory tests remain regression coverage.

