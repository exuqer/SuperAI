# Hive merge rules

New nectar with composition overlap of at least `0.82` updates the existing cell and provenance. Otherwise a local hive cell is created. Weak or duplicate sources are discarded by the bounded-cell limit. Global cloud mass and coordinates are never changed by merging.

