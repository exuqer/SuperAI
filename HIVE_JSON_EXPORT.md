# JSON export

`/reasoning/export` supports `current`, `initial`, `snapshot` and `trace` modes with `full` or `compact` detail. Every export has `schema_version`. The frontend offers formatted/raw rendering, clipboard copy and a JSON download; ORM rows are converted to plain DTOs.

