# Dynamic gravity

Local gravity is stored in `hive_node_states` and mirrored to the hive placement. Query support and shared composition increase it; contradiction and unsupported nodes decrease it. Values are clamped to `[0, 1]`; global cloud mass and global field gravity are never changed by vibration.

