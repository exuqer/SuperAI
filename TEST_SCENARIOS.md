# Test Scenarios

Train the control corpus, repeat `Кот ест рыбу.`, validate distinct `рыбу` scene placements, inspect word character structure, run a V2 hive query, and validate invariants and V1/V2 scene counts.
