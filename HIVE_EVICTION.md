# Eviction

Retention combines stored strength, stability and activation. Weak nodes transition through `WEAKENING` and `EVICTION_CANDIDATE` before becoming `EVICTED`; confirmation steps prevent one bad tick from deleting a node. Evicted nodes remain in snapshots and can be restored explicitly.

